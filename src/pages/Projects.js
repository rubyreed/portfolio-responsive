import React from "react";
import { Container } from "react-bootstrap";

const Projects = () => {
    return (
        <Container>
            <h1>Projects</h1>
        </Container>
    );
};

export default Projects;